---
interface Props {
  repoList: string[];
}

const { repoList }:Props = Astro.props;
---

<script is:inline src="https://tarptaeya.github.io/repo-card/repo-card.js"></script>
{
	repoList.map(entry => (
		<div class="repo-card" data-repo={entry} data-theme="dark-theme"></div>
	))
}

